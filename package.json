{
  "name": "columbia-1-cloudflare",
  "private": true,
  "description": "Cloudflare Containers deployment shim for Columbia-1 (the app itself is Python — see serve.py / Dockerfile).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.16"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
